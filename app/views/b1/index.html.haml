:ruby
  book = Book.find(1)

= page_top_bar book.title, '/a3', :has_search=>true

.page-b1
  .desc
    = truncate(book.desc)
  .sections
    - Section.all.each do |section|
      .item
        .title= link_to section.title, "/b2"
        .sdesc= truncate section.desc, :length=>24