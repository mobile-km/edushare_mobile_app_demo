:ruby
  entries = Entry.all
  search_entries = entries[2..5]


.page-c42

  .top
    = render "/c1/parts/nav"

  .search
    = render "/c41/parts/search"

  .page-search-list
    - search_entries.each do |entry|
      .item
        .title= link_to entry.title, "/c43"