.page-a1

  .top
    = render "/a1/parts/nav"


  .page-book-list
    - Book.all.each do |book|
      .item
        .img
          = link_to image_tag(book.cover_img), "/a2"
        .title
          = link_to book.title, "/a2"
  