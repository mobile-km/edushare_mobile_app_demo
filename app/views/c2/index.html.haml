:ruby
  chapters = Chapter.all
  search_chapters = chapters[0..2]

.page-c2

  .top
    = render "/c1/parts/nav"

  .search
    = render "/c1/parts/search"

  .page-search-list
    - search_chapters.each do |chapter|
      .item
        .title= link_to chapter.title, "/c3"