:ruby
  chapter = Chapter.find(1)

.page-i3
  .page-web-work-list
    .header
      %h3 我的作业列表
    .content.web.work

      .left.content
        .work.about
          .top
            = render "/a1/parts/nav"
          .page-show
            .title= chapter.title
            .content= chapter.content

      .right.content
        = render "/i1/parts/right"